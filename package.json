{
  "name": "flipbits",
  "version": "1.0.0",
  "description": "the best random powered AI to take decisions",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "script": "uglifyjs -o app.js --compress --mangle -- src/**/*.js src/index.js",
    "css": "uglifycss src/**/*.css src/*.css --output app.css",
    "build": "npm run script && npm run css"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maxwellito/flipbits.git"
  },
  "author": "maxwellito",
  "license": "MIT",
  "dependencies": {
    "uglify-js": "^3.3.23",
    "uglifycss": "0.0.29"
  }
}
